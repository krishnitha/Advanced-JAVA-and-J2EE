import javax.servlet.*;
import java.io.*;
import javax.servlet.http.*;
public class err extends HttpServlet
{
	public void doPost(HttpServletRequest req, HttpServletResponse res)throws IOException, ServletException
	{
		res.setContentType("text/html");
		PrintWriter pw=res.getWriter();
		String name=req.getParameter("t1");
		String ps=req.getParameter("t2");
		if(name.equals("")||ps.equals(""))
		{
			res.sendError(404);
		}
		else if(!name.equals("abcd")||!ps.equals("1234"))
		{
			res.sendError(404,"Enter the correct details");
		}
		else
		{
			res.sendRedirect("https://www.aiet.org.in");
		}
	}
}






































































































































































































































































